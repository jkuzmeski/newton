Add a reusable subject example that runs official OpenSim ScaleTool and MarkerPlacer from static C3D, preserves scaled COM/full inertia, translates modern OSIM/VTP into a visually cleared controlled simple-joint MJCF with mesh-derived foot contacts, and proves one-call `ModelBuilder.add_mjcf()` plus Newton-native simulation.
