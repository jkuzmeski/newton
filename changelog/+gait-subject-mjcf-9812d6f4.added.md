Add a reusable subject example that runs official OpenSim ScaleTool and MarkerPlacer from static C3D, translates modern scaled OSIM/VTP output into a controlled simple-joint MJCF, and proves one-call `ModelBuilder.add_mjcf()` loading plus Newton-native Featherstone simulation.
