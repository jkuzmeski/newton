Add a reusable subject example that runs official OpenSim ScaleTool and MarkerPlacer from static C3D, preserves scaled joint centers, COM/full inertia, and full VTP anatomy, translates them into a visually cleared controlled simple-joint MJCF with mesh-derived foot contacts, and proves one-call `ModelBuilder.add_mjcf()` plus Newton-native simulation, with a stable standing inspection time step.
