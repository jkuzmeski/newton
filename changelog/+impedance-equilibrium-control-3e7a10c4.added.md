Add an equilibrium-point impedance controller to the Impedance Instron example, selected with `--control equilibrium`. The virtual leg follows a commanded equilibrium trajectory and a commanded stiffness and damping-ratio profile instead of a measured-force feedforward and hand-tuned engage and release gains, and a unilateral leg ends stance without a scheduled fade. Add `projects.impedance_instron.optimize`, which solves the command with a self-contained CMA-ES against a task-level cost of measured stance impulse and actuator effort, never tracking measured force histories.
