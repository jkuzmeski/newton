Add independently qualified fixed-gain controller screens, bounded fresh-start variants with separate initialization and search seeds, and per-budget fit-quality and work reports. Gains stay constant within each solve; numerical qualification and measured acceptance remain separate. Add GPU-generated bounded local probes with explicit work accounting and no optimality claim. Add replay-only qualification for native-tolerance probe candidates, separate from loss-only ranking.
