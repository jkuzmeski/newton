Pin the Digital Instron midsole model to measured foam mechanics: set the effective Poisson ratio to zero, derive each column's Pasternak coefficient from the equilibrium shear modulus and its rest thickness instead of fitting one shared coefficient, and drop the unreported outer bond so the reported load is the whole summed ground reaction. Refit the shipped shoe against both bench fixtures with no fixture-specific parameter.
